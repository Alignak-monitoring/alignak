[daemon.arbiter-master]
; Listen to external interfaces for WS
host=0.0.0.0
address=arbiter-master
port=7770

[daemon.scheduler-master]
type=scheduler
name=scheduler-master

; Network address
host=0.0.0.0
address=scheduler-master
port=7768

; Realms parameters:
realm=All
manage_sub_realms=1
;
; Modules
modules=inner-retention

[daemon.scheduler-north]
type=scheduler
name=scheduler-north

; Network address
host=0.0.0.0
address=scheduler-north
port=17768

; Realms parameters:
realm=North
manage_sub_realms=1
;
; Modules
modules=inner-retention

[daemon.scheduler-south]
type=scheduler
name=scheduler-south

; Network address
host=0.0.0.0
address=scheduler-south
port=27768

; Realms parameters:
realm=South
manage_sub_realms=1
;
; Modules
modules=inner-retention

[daemon.broker-master]
; Realms parameters:
realm=All
manage_sub_realms=1

; Network address
host=0.0.0.0
address=broker-master
port=7772

; Modules
modules=inner-metrics,webui

[daemon.poller-master]
; Realms parameters:
realm=All
manage_sub_realms=1

; Network address
host=0.0.0.0
address=poller-master
port=7771

; Advanced parameters:
;min_workers=1
;max_workers=1
;processes_by_worker=16
;worker_polling_interval=1

[daemon.receiver-master]
; Realms parameters:
realm=All
manage_sub_realms=1
;
; Network address
host=0.0.0.0
address=receiver-master
port=7773

; Modules
;modules=nsca

[daemon.reactionner-master]
; Realms parameters:
realm=All
manage_sub_realms=1

; Network address
host=0.0.0.0
address=reactionner-master
port=7769

; Advanced parameters:
;min_workers=1
;max_workers=1
;processes_by_worker=8
;worker_polling_interval=1
