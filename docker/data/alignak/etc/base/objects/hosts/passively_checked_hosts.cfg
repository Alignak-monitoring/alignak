define host{
    use                      generic-host
    host_name                not_monitored_host_[0-4-%03d]
    address                  localhost

    realm                    All

    # Not monitored: no active, no passive
    active_checks_enabled    0
    passive_checks_enabled   1

    # No check command
;   check_command            _internal_host_check!0,2!I am changing for %d

    # GPS
    _LOC_LAT 44.054700
    _LOC_LNG 5.180856
}
