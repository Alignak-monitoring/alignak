[Unit]
Description=Alignak arbiter %I daemon instance
PartOf=alignak.service

[Service]
# Environment variables - may be overriden in the /etc/default/alignak
Environment=ALIGNAK_CONFIGURATION_FILE=/usr/local/share/alignak/etc/alignak.ini
Environment=ALIGNAK_USER=alignak
Environment=ALIGNAK_GROUP=alignak
EnvironmentFile=-/etc/default/alignak

Type=forking

# PID file as declared in daemon configuration
PIDFile=/var/run/alignak/arbiter-%i.pid

ExecStartPre=/bin/echo Alignak starting arbiter %i - /usr/local/bin/alignak-arbiter -d -n arbiter-%i -e "${ALIGNAK_CONFIGURATION_FILE}"
ExecStart=/usr/local/bin/alignak-arbiter -d -n arbiter-%i -e "${ALIGNAK_CONFIGURATION_FILE}" --pid_file /var/run/alignak/arbiter-%i.pid --log_file /var/log/alignak/arbiter-%i.pid

# See man systemd.kill
KillMode=control-group
KillSignal=SIGTERM
TimeoutStopSec=30

ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=alignak.service

