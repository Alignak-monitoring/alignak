define scheduler{
    scheduler_name      scheduler-2 ; Just the name
    address             localhost   ; IP or DNS address of the daemon
    port                9990        ; TCP port of the daemon
    realm               TEST

}
