# First a host without realm, not good :)
define host{
  address                        127.0.0.1
  host_name                      test_host_1
  display_name                   +bad_syntax
  hostgroups                     hostgroup_01,up
  use                            generic-host
}
