define host{
        use                     linux,generic-host
	contact_groups		admins
        host_name               tagged_host
        address                 localhost
        poller_tag              TestPollerTag
        check_interval          1
        retry_interval          1
        }
